# Frontend environment variables
# Copy to .env.local and override as needed (never commit .env.local)

# Base URL for all API calls — proxied by Vite in development
VITE_API_BASE_URL=http://localhost:8080/api/v1
